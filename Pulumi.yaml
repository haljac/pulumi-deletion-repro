name: deletion-dependency-demo
description: A minimal Python Pulumi program to demonstrate missing deletion dependencies
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
